// finding length of svg to use dash array
/*
const logo = document.querySelectorAll('#name path');

for(let i=0; i<logo.length; i++){
  console.log(`letter ${i} is ${logo[i].getTotalLength()}`);
}
*/


// Rating Initialization
